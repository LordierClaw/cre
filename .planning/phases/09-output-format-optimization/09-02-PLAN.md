---
phase: 09-output-format-optimization
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified: [src/main/java/com/cre/tools/IntegratedViewBuilder.java]
autonomous: true
requirements: []
---

<objective>
Implement the `IntegratedViewBuilder` to transform a set of retained nodes into a file-centric integrated code view with placeholders.

Purpose: Generate readable, token-efficient code slices with XML-style omission tags.
Output: IntegratedViewBuilder class with AST transformation logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-output-format-optimization/09-01-SUMMARY.md
@.planning/phases/09-output-format-optimization/09-CONTEXT.md
@.planning/phases/09-output-format-optimization/09-RESEARCH.md
@src/main/java/com/cre/core/ast/JavaAstIndexer.java

<interfaces>
From src/main/java/com/cre/tools/model/IntegratedFile.java:
```java
public record IntegratedFile(String origin, String packageName, List<String> imports, String code) {}
```

From src/main/java/com/cre/core/graph/NodeId.java:
```java
public record NodeId(String fullyQualifiedType, String memberSignature, String sourceOrigin) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group nodes and reconstruct file stubs</name>
  <files>src/main/java/com/cre/tools/IntegratedViewBuilder.java</files>
  <read_first>
    - src/main/java/com/cre/core/ast/JavaAstIndexer.java (for AST patterns)
    - src/main/java/com/cre/core/graph/NodeId.java
  </read_first>
  <action>
    - Create `src/main/java/com/cre/tools/IntegratedViewBuilder.java`.
    - Implement `build(Set<NodeId> retained, Set<NodeId> pruned, GraphEngine graph, Path javaSourceRoot)` method.
    - Group all nodes in `retained` and `pruned` by `sourceOrigin`.
    - For each file:
      - Parse the `CompilationUnit` using `AstUtils.JAVA_PARSER`.
      - Reconstruct the skeleton: keep `package` and `imports`.
      - For each `TypeDeclaration`, keep its name and structure, but initially mark all members for pruning.
  </action>
  <acceptance_criteria>
    - `IntegratedViewBuilder` compiles.
    - Class correctly groups nodes by `sourceOrigin`.
  </acceptance_criteria>
  <done>Basic file grouping and parsing infrastructure in place.</done>
</task>

<task type="auto">
  <name>Task 2: Implement member pruning and placeholder injection</name>
  <files>src/main/java/com/cre/tools/IntegratedViewBuilder.java</files>
  <read_first>
    - src/main/java/com/cre/core/ast/JavaAstIndexer.java
    - .planning/phases/09-output-format-optimization/09-CONTEXT.md (D-03, D-04)
  </read_first>
  <action>
    - In `IntegratedViewBuilder`, traverse each `CompilationUnit`'s members (MethodDeclaration, FieldDeclaration).
    - If a member's `NodeId` is NOT in the `retained` set:
      - Assign it a short sequential ID (e.g., `ommitted_01`).
      - Replace the entire member in the AST with a marker string (e.g., `CRE_OMITTED_ommitted_01`).
      - Record the mapping `ommitted_01 -> full_node_id_string` in `nodeIdMap`.
    - If a member is a nested class, recurse into its members.
  </action>
  <acceptance_criteria>
    - Members not in `retainedSet` are replaced by marker strings in the generated `code`.
    - `nodeIdMap` contains mappings for all pruned members.
  </acceptance_criteria>
  <done>Pruned methods and fields are replaced by placeholders in the integrated view.</done>
</task>

<task type="auto">
  <name>Task 3: Implement method call replacement</name>
  <files>src/main/java/com/cre/tools/IntegratedViewBuilder.java</files>
  <read_first>
    - src/main/java/com/cre/core/ast/JavaAstIndexer.java (resolveCallee pattern)
    - .planning/phases/09-output-format-optimization/09-CONTEXT.md (D-04)
  </read_first>
  <action>
    - In `IntegratedViewBuilder`, for each RETAINED method:
      - Find all `MethodCallExpr` nodes.
      - Attempt to resolve the callee `NodeId` using the same logic as `JavaAstIndexer.resolveCallee`.
      - If the resolved `NodeId` is in the `pruned` set (meaning it was pruned from this slice):
        - Assign it a short ID (if not already assigned).
        - Replace the `MethodCallExpr` node with a placeholder marker `CRE_OMITTED_ommitted_NN`.
    - Final Step: Before returning, perform a regex replacement on the final `code` string: `CRE_OMITTED_(ommitted_\d+)` -> `<$1/>`.
  </action>
  <acceptance_criteria>
    - Method calls to pruned nodes are replaced by `<ommitted_NN/>` tags in the final code.
    - All tags are present in the `nodeIdMap`.
  </acceptance_criteria>
  <done>Method calls to pruned code are properly replaced with tags per D-04.</done>
</task>

</tasks>

<verification>
Unit test for IntegratedViewBuilder (to be created in Task 1 or as a separate test class).
</verification>

<success_criteria>
- IntegratedViewBuilder correctly groups code by file.
- Non-retained members are replaced by `<ommitted_NN/>`.
- Method calls to non-retained nodes are replaced by `<ommitted_NN/>`.
- node_id_map accurately maps short IDs to full NodeIds.
</success_criteria>

<output>
After completion, create `.planning/phases/09-output-format-optimization/09-02-SUMMARY.md`
</output>
