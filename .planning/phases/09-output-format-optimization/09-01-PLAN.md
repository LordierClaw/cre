---
phase: 09-output-format-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/main/java/com/cre/tools/model/GetContextResponse.java, src/main/java/com/cre/tools/model/IntegratedFile.java, src/test/java/com/cre/tools/GetContextSchemaTest.java]
autonomous: true
requirements: []
---

<objective>
Define the optimized output models (v2) and update the schema validation tests to establish the new contract.

Purpose: Move from verbose multi-list JSON to a compact file-centric integrated view.
Output: Updated GetContextResponse, new IntegratedFile record, and updated schema tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-output-format-optimization/09-CONTEXT.md
@.planning/phases/09-output-format-optimization/09-RESEARCH.md

<interfaces>
From src/main/java/com/cre/tools/model/GetContextResponse.java:
```java
public record GetContextResponse(
    @JsonProperty("slice_version") String sliceVersion,
    Map<String, Object> metadata,
    List<Map<String, Object>> nodes,
    List<Map<String, Object>> edges,
    @JsonProperty("sliced_code") List<Map<String, Object>> slicedCode,
    List<Placeholder> placeholders) {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define IntegratedFile and update GetContextResponse</name>
  <files>src/main/java/com/cre/tools/model/IntegratedFile.java, src/main/java/com/cre/tools/model/GetContextResponse.java</files>
  <read_first>
    - src/main/java/com/cre/tools/model/GetContextResponse.java
    - .planning/phases/09-output-format-optimization/09-CONTEXT.md (per D-01, D-05, D-06)
  </read_first>
  <action>
    - Create `src/main/java/com/cre/tools/model/IntegratedFile.java` as a record with:
      - `origin` (String)
      - `packageName` (String)
      - `imports` (List of String)
      - `code` (String)
    - Update `src/main/java/com/cre/tools/model/GetContextResponse.java`:
      - Remove `nodes`, `edges`, `slicedCode`, and `placeholders`.
      - Add `@JsonProperty("integrated_files") List<IntegratedFile> integratedFiles`.
      - Add `@JsonProperty("node_id_map") Map<String, String> nodeIdMap`.
      - Keep `sliceVersion` and `metadata`.
  </action>
  <acceptance_criteria>
    - `IntegratedFile.java` exists with all four fields.
    - `GetContextResponse.java` contains `integratedFiles` and `nodeIdMap`.
    - `GetContextResponse.java` no longer contains `nodes`, `edges`, `slicedCode`, or `placeholders`.
    - Code compiles: `mvn compile`
  </acceptance_criteria>
  <done>Output models updated to v2 schema per D-01, D-05, D-06.</done>
</task>

<task type="auto">
  <name>Task 2: Update GetContextSchemaTest to v2</name>
  <files>src/test/java/com/cre/tools/GetContextSchemaTest.java, src/main/java/com/cre/tools/GetContextTool.java</files>
  <read_first>
    - src/test/java/com/cre/tools/GetContextSchemaTest.java
    - src/main/java/com/cre/tools/GetContextTool.java
  </read_first>
  <action>
    - Update `GetContextTool.java`: change `SLICE_VERSION` constant to `"cre.slice.v2"`.
    - Temporarily stub `execute` and `expand` in `GetContextTool.java` to return a dummy `GetContextResponse` with empty `integratedFiles` and `nodeIdMap` so tests can compile.
    - Update `GetContextSchemaTest.java`:
      - Change expectation for `slice_version` to `"cre.slice.v2"`.
      - Remove checks for `nodes`, `edges`, `sliced_code`, and `placeholders`.
      - Add checks that `integrated_files` is an array and `node_id_map` is an object.
  </action>
  <acceptance_criteria>
    - `mvn test -Dtest=GetContextSchemaTest` passes (even with empty data, as long as schema matches).
    - `GetContextTool.SLICE_VERSION` is `"cre.slice.v2"`.
  </acceptance_criteria>
  <done>Schema validation tests updated to expect v2 structure.</done>
</task>

</tasks>

<verification>
mvn test -Dtest=GetContextSchemaTest
</verification>

<success_criteria>
- GetContextResponse uses v2 schema (integrated_files, node_id_map).
- GetContextSchemaTest passes with the new structure.
- SLICE_VERSION is updated to v2.
</success_criteria>

<output>
After completion, create `.planning/phases/09-output-format-optimization/09-01-SUMMARY.md`
</output>
