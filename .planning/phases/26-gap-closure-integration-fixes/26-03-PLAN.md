---
phase: 26-gap-closure-integration-fixes
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified: [src/test/java/com/cre/core/IntegrationGapsTest.java]
autonomous: true
requirements: [GAP-01]
must_haves:
  truths:
    - "Integration tests prove that Records are correctly optimized (retained when relevant, pruned when not)."
    - "Integration tests prove that FQN parameter signatures match simple-name indexed IDs."
  artifacts:
    - path: "src/test/java/com/cre/core/IntegrationGapsTest.java"
      provides: "Regression tests for gap closure"
---

<objective>
Verify the fixes for signature normalization and Record support through integration tests.

Purpose: Prevents regression and ensures the end-to-end flow (Index -> Search -> Optimize) works for identified edge cases.
Output: A new integration test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-gap-closure-integration-fixes/26-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement regression tests for integration gaps</name>
  <files>src/test/java/com/cre/core/IntegrationGapsTest.java</files>
  <behavior>
    - Test Case 1: Define a Java Record with methods and Javadoc. Index it, then request context for one of its methods. Verify that the Record's Javadoc and the relevant method are retained, while other members are pruned.
    - Test Case 2: Define a class with a method using FQN parameter (e.g., `void execute(java.util.List<String> list)`). Index it, then request context using a simple-name signature (`execute(List)`). Verify that the body is correctly included in the output.
  </behavior>
  <action>
    Create `IntegrationGapsTest.java` using JUnit and the existing `GraphEngine` and `CreService` infrastructure.
    Use temporary files or string-based indexing if supported by the test harness.
  </action>
  <verify>
    <automated>mvn test -Dtest=IntegrationGapsTest</automated>
  </verify>
  <done>All regression tests pass, confirming gap closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test Data -> System | Tests use synthetic code; ensure the code is valid Java and represents the target patterns. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Information Disclosure | Test Logs | accept | No PII in test data. |
</threat_model>

<verification>
Run `mvn test -Dtest=IntegrationGapsTest`.
</verification>

<success_criteria>
1. All tests in `IntegrationGapsTest` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/26-gap-closure-integration-fixes/26-03-SUMMARY.md`
</output>
