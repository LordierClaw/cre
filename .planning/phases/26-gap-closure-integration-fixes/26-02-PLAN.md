---
phase: 26-gap-closure-integration-fixes
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified: [src/main/java/com/cre/core/service/CreServiceImpl.java]
autonomous: true
requirements: [GAP-01]
must_haves:
  truths:
    - "CreServiceImpl uses AstUtils for consistent signature generation."
    - "CreServiceImpl correctly handles RecordDeclaration in transformation and comment pruning."
  artifacts:
    - path: "src/main/java/com/cre/core/service/CreServiceImpl.java"
      provides: "Record support and unified signature matching"
---

<objective>
Align CreServiceImpl with the unified signature normalization and add first-class support for Records.

Purpose: Ensures that modern Java features (Records) are optimized correctly and that reconstruction works for all signatures.
Output: Refactored CreServiceImpl.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-gap-closure-integration-fixes/26-01-SUMMARY.md

<interfaces>
From src/main/java/com/cre/core/ast/AstUtils.java (created in 26-01):
```java
public static String getMethodSignature(MethodDeclaration md);
public static String getConstructorSignature(ConstructorDeclaration cd);
```

From src/main/java/com/cre/core/service/CreServiceImpl.java:
```java
private Set<String> transformWithRelevance(CompilationUnit cu, String typeFqn, List<String> gathered, ContextOptions options);
private void pruneComments(CompilationUnit cu, Set<String> gatheredIds, String targetNodeId);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align CreServiceImpl with unified signatures</name>
  <files>src/main/java/com/cre/core/service/CreServiceImpl.java</files>
  <action>
    1. Update `methodSignature(MethodDeclaration md)` to delegate to `AstUtils.getMethodSignature(md)`.
    2. Update `constructorSignature(ConstructorDeclaration cd)` to delegate to `AstUtils.getConstructorSignature(cd)`.
    3. Update `calculateNodeId` to handle `CompactConstructorDeclaration` (delegating to `AstUtils`).
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>Service uses identical ID generation logic as the Indexer.</done>
</task>

<task type="auto">
  <name>Task 2: Add RecordDeclaration support to optimization logic</name>
  <files>src/main/java/com/cre/core/service/CreServiceImpl.java</files>
  <action>
    1. Update `transformWithRelevance`: Add an `else if (td instanceof RecordDeclaration rd)` block.
    2. Within the block, iterate over `rd.getMembers()` (methods, constructors) and `rd.getParameters()` (components indexed as fields).
    3. Match gathered nodes and mark them for keeping.
    4. Update `pruneComments`: Ensure it identifies `RecordDeclaration` as a type parent and correctly calculates node IDs for its members and components.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>Records are no longer "invisible" to the optimizer; they undergo surgical pruning like classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Graph -> Service | Service relies on IDs stored in the graph; any drift in normalization will break matching. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Denial of Service | CreServiceImpl | mitigate | Ensure Record component pruning doesn't enter infinite loops or crash on records with no components. |
</threat_model>

<verification>
Check `CreServiceImpl` for `RecordDeclaration` and `AstUtils` usage.
</verification>

<success_criteria>
1. Code compiles.
2. `transformWithRelevance` and `pruneComments` explicitly handle `RecordDeclaration`.
</success_criteria>

<output>
After completion, create `.planning/phases/26-gap-closure-integration-fixes/26-02-SUMMARY.md`
</output>
