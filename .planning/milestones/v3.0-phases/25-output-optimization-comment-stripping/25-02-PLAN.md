---
phase: 25
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified: [src/main/java/com/cre/core/service/DefaultContextPostProcessor.java, src/test/java/com/cre/core/service/DefaultContextPostProcessorTest.java]
autonomous: true
requirements: [OPT-01]

must_haves:
  truths:
    - "Consecutive empty lines (3+) are collapsed into a single empty line."
    - "XML-style markers (<omitted_.../>) are placed on their own lines."
    - "File blocks have leading/trailing whitespace trimmed without affecting internal code indentation."
  artifacts:
    - path: "src/main/java/com/cre/core/service/DefaultContextPostProcessor.java"
      provides: "Regex-based context normalization"
    - path: "src/test/java/com/cre/core/service/DefaultContextPostProcessorTest.java"
      provides: "Unit tests for formatting cleanup"
---

<objective>
Implement regex-based formatting cleanup in DefaultContextPostProcessor to optimize the physical layout of the reconstructed context.

Purpose: Improve readability and further reduce token usage by eliminating whitespace bloat and fixing formatting artifacts caused by AST node removals.
Output: Functional post-processor with robust normalization logic and supporting unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-output-optimization-comment-stripping/25-CONTEXT.md
@.planning/phases/25-output-optimization-comment-stripping/25-RESEARCH.md
@src/main/java/com/cre/core/service/DefaultContextPostProcessor.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Regex Normalization Logic</name>
  <files>src/main/java/com/cre/core/service/DefaultContextPostProcessor.java</files>
  <action>
    - Update `process(String context)` implementation in `DefaultContextPostProcessor`.
    - Implement the following normalization steps (D-04, D-05, D-06, D-12):
      1. Collapse 3+ consecutive newlines (including whitespace-only lines) into exactly 2 newlines (one empty line).
      2. Ensure `<omitted_functions/>` and `<omitted_properties/>` markers are on their own line. If a marker is "inline" (preceded or followed by non-newline characters), insert a newline.
      3. Trim whitespace around `<file>` blocks: remove leading whitespace after `<file...>` and trailing whitespace before `</file>`.
      4. Use multi-line regex patterns (e.g., `(?m)`) or specific escape sequences to avoid destroying indentation inside gathered methods.
  </action>
  <verify>
    - `mvn clean compile` passes.
    - Inspect the logic to ensure it targets specific "bad smells" without being over-aggressive.
  </verify>
  <done>Regex normalization logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Create Unit Tests for Post-Processor</name>
  <files>src/test/java/com/cre/core/service/DefaultContextPostProcessorTest.java</files>
  <action>
    - Create a new unit test class `DefaultContextPostProcessorTest`.
    - Add test cases for:
      - Collapsing multiple empty lines.
      - Fixing inline markers.
      - Trimming file blocks.
      - Preservation of internal method indentation (ensure regex doesn't `trim()` the entire string's lines).
      - Mixed scenarios with comments and multiple files.
  </action>
  <verify>
    - `mvn test -Dtest=DefaultContextPostProcessorTest` passes.
  </verify>
  <done>Post-processor unit tests created and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Post-Processor → Agent | Sanitized output is delivered to the agent, reducing noise and potential parsing errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Tampering | DefaultContextPostProcessor | mitigate | Regex patterns are tested against a variety of inputs to ensure they don't accidentally "eat" or corrupt code content. |
</threat_model>

<verification>
- Unit tests covering all regex scenarios.
</verification>

<success_criteria>
- Reconstructed context is free of excessive whitespace and formatting artifacts.
- Markers are consistently placed on their own lines.
</success_criteria>

<output>
After completion, create `.planning/phases/25-output-optimization-comment-stripping/25-02-SUMMARY.md`
</output>
