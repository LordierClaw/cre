---
phase: 09-output-format-optimization
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified: [src/main/java/com/cre/tools/GetContextTool.java, src/test/java/com/cre/e2e/RealProjectE2ETest.java]
autonomous: true
requirements: [PH9-01, PH9-02, PH9-03]
must_haves:
  truths:
    - "GetContextTool produces optimized v2 output format"
    - "System-wide E2E tests pass with the new format"
    - "Old response construction logic is completely removed"
  artifacts:
    - path: "src/main/java/com/cre/tools/GetContextTool.java"
      provides: "Refactored tool with builder integration"
    - path: "src/test/java/com/cre/e2e/RealProjectE2ETest.java"
      provides: "E2E verification of v2 output"
  key_links:
    - from: "src/main/java/com/cre/tools/GetContextTool.java"
      to: "src/main/java/com/cre/tools/IntegratedViewBuilder.java"
      via: "Method call to build()"
---

<objective>
Integrate the `IntegratedViewBuilder` into the `GetContextTool` and verify system-wide correctness with E2E tests.

Purpose: Finalize the optimized format in the live tool surface.
Output: Refactored GetContextTool and updated E2E tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-output-format-optimization/09-02-SUMMARY.md
@.planning/phases/09-output-format-optimization/09-CONTEXT.md
@src/main/java/com/cre/tools/GetContextTool.java
@src/test/java/com/cre/e2e/RealProjectE2ETest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Builder into GetContextTool and Remove Stubs</name>
  <files>src/main/java/com/cre/tools/GetContextTool.java</files>
  <read_first>
    - src/main/java/com/cre/tools/GetContextTool.java
    - src/main/java/com/cre/tools/IntegratedViewBuilder.java
  </read_first>
  <action>
    - **CRITICAL:** Remove the temporary stubs for `execute` and `expand` created in Plan 01.
    - Modify `GetContextTool.java`:
      - Remove the old response construction logic (generating `nodes`, `edges`, `sliced_code` lists).
      - Replace it with a call to `IntegratedViewBuilder.build(retainedSet, prunedSet, graph, javaSourceRoot)`.
      - Use the builder's result (IntegratedFile list and nodeIdMap) to populate the `GetContextResponse`.
      - Ensure `expand` and `mergeSlices` are refactored to merge node sets before calling the builder.
  </action>
  <verify>
    <automated>mvn test -Dtest=GetContextSchemaTest</automated>
  </verify>
  <done>GetContextTool now produces optimized v2 output format. Stubs from Plan 01 are replaced with real implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Update and verify RealProjectE2ETest</name>
  <files>src/test/java/com/cre/e2e/RealProjectE2ETest.java</files>
  <read_first>
    - src/test/java/com/cre/e2e/RealProjectE2ETest.java
  </read_first>
  <action>
    - Update `RealProjectE2ETest.java`:
      - Adjust assertions on `GetContextResponse` to use the new `integrated_files` and `node_id_map` fields.
      - Verify that the integrated code contains the expected logic and tags.
      - Verify that `node_id_map` correctly maps tags to NodeIds.
  </action>
  <verify>
    <automated>mvn test -Dtest=RealProjectE2ETest</automated>
  </verify>
  <done>E2E tests verified for the new output format.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification and model cleanup</name>
  <files>src/main/java/com/cre/tools/model/GetContextResponse.java, src/main/java/com/cre/tools/GetContextTool.java</files>
  <read_first>
    - src/main/java/com/cre/tools/GetContextTool.java
  </read_first>
  <action>
    - Review the codebase for unused imports or methods (e.g., old `edgeKey`, `placeholderKey` in `GetContextTool`).
    - Finalize formatting of the code snippets to ensure class paths are compact (per D-08).
    - Run all tests: `mvn test`.
  </action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>Phase 09 complete with clean, verified implementation.</done>
</task>

</tasks>

<verification>
mvn test
</verification>

<success_criteria>
- Tool output matches D-01 through D-08 in CONTEXT.md.
- E2E tests pass on the real target project.
- Redundant lists (nodes, edges, placeholders) are removed.
</success_criteria>

<output>
After completion, create `.planning/phases/09-output-format-optimization/09-03-SUMMARY.md`
</output>
