---
phase: 10-enhanced-xml-wrapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/main/java/com/cre/tools/IntegratedViewBuilder.java, src/test/java/com/cre/tools/IntegratedViewBuilderTest.java]
autonomous: true
requirements: [REQ-10.2, REQ-10.3, REQ-10.4]

must_haves:
  truths:
    - "Indentation is preserved using LexicalPreservingPrinter"
    - "Pruned imports are replaced by a single <ommitted_import/>"
    - "Pruned fields are replaced by a single <ommitted_properties/>"
    - "Pruned methods are replaced by a single <ommitted_functions/>"
    - "Pruned method calls are replaced by <ommitted_code id=\"...\" description=\"\"/>"
  artifacts:
    - path: "src/main/java/com/cre/tools/IntegratedViewBuilder.java"
      provides: "AST transformation with category-based grouping"
    - path: "src/test/java/com/cre/tools/IntegratedViewBuilderTest.java"
      provides: "Verification of new semantic XML tags"
  key_links:
    - from: "src/main/java/com/cre/tools/IntegratedViewBuilder.java"
      to: "JavaParser.LexicalPreservingPrinter"
      via: "printer setup and print calls"
---

<objective>
Update IntegratedViewBuilder to use LexicalPreservingPrinter and implement category-based grouping for pruned code. This plan focuses on the core AST transformation and regex logic to produce the new semantic XML tags while maintaining source indentation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-enhanced-xml-wrapping/10-CONTEXT.md
@.planning/phases/10-enhanced-xml-wrapping/10-RESEARCH.md
@src/main/java/com/cre/tools/IntegratedViewBuilder.java
@src/test/java/com/cre/tools/IntegratedViewBuilderTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Refactor IntegratedViewBuilder for Lexical Preservation and Category Grouping</name>
  <files>src/main/java/com/cre/tools/IntegratedViewBuilder.java</files>
  <action>
    - Update `build` method to use `LexicalPreservingPrinter.setup(cu)` immediately after parsing.
    - Implement import pruning: If any imports are pruned, insert a marker `CRE_OM_IMPORT` at the end of the import block.
    - Implement properties grouping: If any `FieldDeclaration`s in a class are pruned, replace the FIRST pruned field with `CRE_OM_PROPS` and remove all other pruned fields.
    - Implement functions grouping: If any `MethodDeclaration`s in a class are pruned, insert `CRE_OM_FUNCS` at the end of the class body (after all other members) and remove all pruned methods.
    - Update `transformMethodCalls` to use `CRE_OM_CODE_ommitted_NN` for call-site replacements.
    - Use `LexicalPreservingPrinter.print(cu)` to generate the code string.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>IntegratedViewBuilder uses LexicalPreservingPrinter and inserts category markers in the AST.</done>
</task>

<task type="auto">
  <name>Task 1.2: Update Regex Transformation for Semantic XML Tags</name>
  <files>src/main/java/com/cre/tools/IntegratedViewBuilder.java</files>
  <action>
    Update the regex replacement logic in `IntegratedViewBuilder.build()`:
    - `CRE_OM_IMPORT` -> `<ommitted_import/>` (per D-04)
    - `CRE_OM_PROPS` -> `<ommitted_properties/>` (per D-05)
    - `CRE_OM_FUNCS` -> `<ommitted_functions/>` (per D-06)
    - `CRE_OM_CODE_(ommitted_\d+)` -> `<ommitted_code id="$1" description=""/>` (per D-07, D-08)
    Ensure regex matches handles markers inserted as field declarations or name expressions.
  </action>
  <verify>
    <automated>mvn test -Dtest=IntegratedViewBuilderTest</automated>
  </verify>
  <done>Regex transformation produces the new semantic XML tags.</done>
</task>

<task type="auto">
  <name>Task 1.3: Update and Verify IntegratedViewBuilderTest</name>
  <files>src/test/java/com/cre/tools/IntegratedViewBuilderTest.java</files>
  <action>
    Update `IntegratedViewBuilderTest.java` to match the new format:
    - Update `test_build_integrated_view_with_pruning` to expect `<ommitted_properties/>` instead of `<ommitted_01/>` for pruned fields.
    - Update `test_method_call_replacement` to expect `<ommitted_code id="ommitted_NN" description=""/>`.
    - Add a new test case verifying `<ommitted_functions/>` grouping for multiple pruned methods.
    - Add a test case for `<ommitted_import/>` if imports are pruned.
  </action>
  <verify>
    <automated>mvn test -Dtest=IntegratedViewBuilderTest</automated>
  </verify>
  <done>Tests pass and verify the new XML format and grouping logic.</done>
</task>

</tasks>

<verification>
Check for correct indentation in `IntegratedViewBuilderTest` output and verify the presence of new XML tags.
</verification>

<success_criteria>
- `IntegratedViewBuilder` produces semantic grouping tags for imports, fields, and methods.
- Call-site omissions use the descriptive `<ommitted_code/>` format.
- Original code indentation is preserved in the output.
</success_criteria>

<output>
After completion, create `.planning/phases/10-enhanced-xml-wrapping/10-01-SUMMARY.md`
</output>
