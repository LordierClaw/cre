# Validation Plan: Phase 17.2 - Context Depth & Indexer Fixes

## Objective
Verify strict reachability for `depth=0, 1` and functional deep traversal for `depth=2`.

## Automated Verification

### 1. Unit Tests (New)
-   **File**: `src/test/java/com/cre/repro/ReproPhase17Test.java`
-   **Criteria**: 
    -   `reproduce_depth_1_all_functions_bug` must pass (other methods should be pruned).
    -   `reproduce_depth_2_bug` must pass (should include deep callees).
-   **Command**: `mvn test -Dtest=ReproPhase17Test`

### 2. Integration Script (New)
-   **File**: `scripts/test_context_depth.py`
-   **Target**: `/home/hainn/blue/code/cre-test-project`
-   **Command**: `python3 scripts/test_context_depth.py`
-   **Criteria**: 
    -   Verify `depth=0` output size and contents.
    -   Verify `depth=1` output size and contents.
    -   Verify `depth=2` output size and contents.

## Manual Verification
- Execute `test_mcp.sh` and select option 1 or 5 with varying depths to visually inspect the tool output.
