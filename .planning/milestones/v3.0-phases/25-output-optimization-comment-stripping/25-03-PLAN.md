---
phase: 25
plan: 03
type: execute
wave: 3
depends_on: [25-02]
files_modified: [src/test/java/com/cre/e2e/OutputOptimizationE2ETest.java]
autonomous: true
requirements: [OPT-01]

must_haves:
  truths:
    - "E2E verification proves comment stripping and formatting optimizations work together."
    - "Context output for a large project remains structurally sound and readable."
  artifacts:
    - path: "src/test/java/com/cre/e2e/OutputOptimizationE2ETest.java"
      provides: "E2E verification of optimization features"
---

<objective>
Verify the complete output optimization chain through E2E tests and ensure high-quality context reconstruction for large project scenarios.

Purpose: Final validation of Phase 25 goals, ensuring that the combination of traversal capping, comment pruning, and regex normalization produces the expected token efficiency and readability.
Output: E2E test suite confirming optimization effectiveness.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-output-optimization-comment-stripping/25-CONTEXT.md
@.planning/phases/25-output-optimization-comment-stripping/25-RESEARCH.md
@src/test/java/com/cre/e2e/ContextReconstructionE2ETest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement E2E Tests for Optimization</name>
  <files>src/test/java/com/cre/e2e/OutputOptimizationE2ETest.java</files>
  <action>
    - Create `OutputOptimizationE2ETest` using existing fixture projects.
    - Test Case 1: Comment Pruning. Request a symbol and verify that:
      - The target function has its Javadocs and internal comments.
      - Neighbors (skeletons) have NO comments.
      - The target class has its class-level Javadoc.
    - Test Case 2: Formatting Quality. Verify that:
      - Output contains no 3+ consecutive newlines.
      - Markers are on their own lines.
      - Total character count (proxy for tokens) is lower compared to non-optimized output (can compare with a baseline or just assert presence of expected pruned state).
    - Test Case 3: Traversal Capping. Use a large graph (or simulate one via multiple dependencies) and verify that the number of gathered nodes scales according to the dynamic cap for the requested depth.
  </action>
  <verify>
    - `mvn test -Dtest=OutputOptimizationE2ETest` passes.
  </verify>
  <done>E2E verification tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Service | Tests verify that the service behaves correctly and securely under large project conditions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Regression | Output Optimization | mitigate | E2E tests ensure that future changes to traversal or pruning don't re-introduce whitespace bloat or token explosion. |
</stride_model>

<verification>
- All E2E tests pass.
- Manual inspection of a sample optimized output to confirm "look and feel".
</verification>

<success_criteria>
- Optimized output is measurably smaller in size than non-optimized equivalent.
- Code blocks remain valid Java syntax and are easily readable by the agent.
</success_criteria>

<output>
After completion, create `.planning/phases/25-output-optimization-comment-stripping/25-03-SUMMARY.md`
</output>
