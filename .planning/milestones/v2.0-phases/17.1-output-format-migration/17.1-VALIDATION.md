# Validation Plan: Phase 17.1 - Output Format Migration

## Objective
Verify the migration from `<ClassName>` tags to `<file name="package.Class">` tags.

## Automated Verification

### 1. Existing Unit Tests
-   **File**: `src/test/java/com/cre/core/service/CreServiceTest.java`
-   **Update**: Update assertions that currently check for `<ClassName>` to check for `<file name="package.Class">`.
-   **Command**: `mvn test -Dtest=CreServiceTest`

### 2. Integration Script (New)
-   **File**: `scripts/test_context_format.py`
-   **Target**: `/home/hainn/blue/code/cre-test-project`
-   **Command**: `python3 scripts/test_context_format.py`
-   **Criteria**: 
    -   Verify presence of `<file name="com.bookstore.controller.AdminBookController">` tag.
    -   Verify absence of `<AdminBookController>` tag.
    -   Verify nested structure (imports, package, class body) within `<file>` tag.

## Manual Verification
-   Execute `test_mcp.sh` and select option 1 or 5 to visually inspect the tool output.
