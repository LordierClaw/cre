# Phase 22: Polymorphism & Inheritance

## Objective
Correctly resolve method calls and field access through class hierarchies.
Ensure that inheritance is reflected in the graph and traversal.

## Requirements
- **SYMB-03**: Resolve symbols across class hierarchy (parent classes, interfaces).
- **SYMB-07**: Identify potential implementations for interface/abstract calls.

## Status
- **Current Position**: Phase 21 complete (Overloading & Generics handled).
- **Goal**: Handle multi-level inheritance and interface implementation.

## Success Criteria
1. Symbols from parent classes are indexed and linked.
2. Method calls through interface/abstract class types correctly identify the defined signature in the parent.
