---
phase: 25
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/main/java/com/cre/core/service/CreServiceImpl.java]
autonomous: true
requirements: [OPT-01]

must_haves:
  truths:
    - "Traversal capping scales dynamically with depth (e.g., depth 1 allows more nodes than depth 4)."
    - "Comments are preserved for gathered nodes (full bodies) but stripped for skeleton nodes."
    - "Class-level Javadocs are stripped unless the class is the primary target."
  artifacts:
    - path: "src/main/java/com/cre/core/service/CreServiceImpl.java"
      provides: "Surgical comment pruning and dynamic traversal capping"
  key_links:
    - from: "gatherNodesOrdered"
      to: "Dynamic capping formula"
      via: "depth-based limit selection"
    - from: "buildIntegratedView"
      to: "pruneComments"
      via: "passing gatheredIds and targetFqn"
---

<objective>
Refactor CreServiceImpl to implement Breadth-First Priority traversal with dynamic node capping and surgical comment pruning.

Purpose: Reduce token usage in large projects by limiting exploration breadth at higher depths and removing non-essential comments while preserving those in execution-critical code blocks.
Output: Optimized CreServiceImpl with context-aware pruning and traversal logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-output-optimization-comment-stripping/25-CONTEXT.md
@.planning/phases/25-output-optimization-comment-stripping/25-RESEARCH.md
@src/main/java/com/cre/core/service/CreServiceImpl.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor gatherNodesOrdered with Dynamic Capping</name>
  <files>src/main/java/com/cre/core/service/CreServiceImpl.java</files>
  <action>
    - Update `gatherNodesOrdered(GraphEngine graph, String start, int maxDepth)` to calculate a dynamic node cap based on `maxDepth`.
    - Formula (per D-09/RESEARCH): `int cap = (maxDepth <= 1) ? 150 : (maxDepth <= 3) ? 100 : 50;`.
    - Ensure the traversal strictly follows Breadth-First Priority (D-10). The current implementation uses a `Queue` and `distances` map, which is correct, but ensure the `visited.size() < cap` check is applied correctly to allow completing the current depth if possible, or at least stopping precisely at the cap.
  </action>
  <verify>
    - Run existing tests to ensure no regressions in basic traversal.
    - Check that MAX_GATHER_NODES is replaced by the dynamic cap in this method.
  </verify>
  <done>Dynamic capping implemented in gatherNodesOrdered.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Surgical Comment Pruning</name>
  <files>src/main/java/com/cre/core/service/CreServiceImpl.java</files>
  <action>
    - Refactor `pruneComments(CompilationUnit cu)` to `pruneComments(CompilationUnit cu, Set<String> gatheredIds, String targetNodeId)`.
    - Implement logic per D-01, D-02, D-03:
      - Keep ALL comments (internal and Javadoc) for nodes present in `gatheredIds`.
      - For the target class (extracted from `targetNodeId`), keep the Javadoc.
      - For all other nodes (skeletons), strip all comments.
    - Add a private helper `calculateNodeId(Node node, String typeFqn)` to generate IDs matching the format in `transformWithRelevance` (e.g., `fqn::signature` for methods, `fqn::field:name` for fields).
    - Handle class-level Javadocs: check if the node is a `TypeDeclaration` and if its FQN matches the target FQN derived from `targetNodeId`.
  </action>
  <verify>
    - Verify `pruneComments` logic via internal unit tests (if possible) or by inspecting the results in subsequent tasks.
  </verify>
  <done>Surgical comment pruning logic implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Pruning and Traversal into Service Flows</name>
  <files>src/main/java/com/cre/core/service/CreServiceImpl.java</files>
  <action>
    - Update `buildIntegratedView` to pass `new HashSet<>(gathered)` and the `startNodeId` (passed from `getReconstructedContext`) to the new `pruneComments`.
    - Update `getFileStructure` to pass an empty set of gathered IDs and the target FQN to `pruneComments` (ensuring all comments are stripped for skeletons except possibly the target class Javadoc).
    - Ensure `LexicalPreservingPrinter.setup(cu)` is called *before* `pruneComments` in `buildIntegratedView` to maintain formatting (D-11).
  </action>
  <verify>
    - `mvn clean compile` passes.
    - Basic E2E test runs without crashing.
  </verify>
  <done>CreServiceImpl refactoring complete and wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Traversal → Output | Traversal limits prevent resource exhaustion (token explosion) from large projects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Denial of Service | gatherNodesOrdered | mitigate | Dynamic node capping prevents unbounded graph traversal and large output generation. |
| T-25-02 | Information Leakage | pruneComments | mitigate | Strategic comment stripping removes potentially sensitive internal developer comments from non-essential code blocks. |
</threat_model>

<verification>
- Unit tests for dynamic capping logic.
- Visual inspection of logs/output for pruned vs preserved comments.
</verification>

<success_criteria>
- CreServiceImpl uses depth-aware caps for node gathering.
- Reconstructed context contains comments only for relevant, full-body functions.
</success_criteria>

<output>
After completion, create `.planning/phases/25-output-optimization-comment-stripping/25-01-SUMMARY.md`
</output>
