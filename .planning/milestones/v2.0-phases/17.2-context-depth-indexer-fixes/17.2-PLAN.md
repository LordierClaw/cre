---
requirements:
  - "CTX-02: Optimized context output (XML-like tagging)"
  - "BUG-02: Robustness improvements"
depends_on:
  - "Phase 17.1"
must_haves:
  - "depth=0 must only return the start node"
  - "Strict Reachability: irrelevant methods must be pruned"
  - "Depth > 2 must correctly resolve deeper callees (e.g. Repository save)"
---

# Plan: Phase 17.2 - Context Depth & Indexer Fixes

## Objective
Fix `get_context` depth logic and improve `JavaAstIndexer` to support deep traversal.

<task id="1">
<files>
<file>src/main/java/com/cre/core/service/CreServiceImpl.java</file>
</files>
<action>
Refactor `gatherNodesOrdered` to filter edges.
Only follow `CALLS`, `USES_FIELD`, and `IMPLEMENTS`.
Prevent following `BELONGS_TO` upwards unless starting from a Type.
</action>
<verify>
Unit test `ReproPhase17Test` should show only the target method at depth 0.
</verify>
<done>false</done>
</task>

<task id="2">
<files>
<file>src/main/java/com/cre/core/service/CreServiceImpl.java</file>
</files>
<action>
Refactor `transformWithRelevance` to strictly prune any `MethodDeclaration` or `ConstructorDeclaration` not in the `retainedNodes` set.
Also strictly prune Fields not in the set (via `USES_FIELD` edges).
</action>
<verify>
`ReproPhase17Test` should confirm non-callee methods are pruned at depth 1 and 2.
</verify>
<done>false</done>
</task>

<task id="3">
<files>
<file>src/main/java/com/cre/core/ast/JavaAstIndexer.java</file>
</files>
<action>
Improve `resolveCallee` and `inferArgumentType` to handle complex signatures and ensure consistent `::` delimiter usage.
Specifically, address the `?` fallback in `inferArgumentType`.
</action>
<verify>
`ReproPhase17Test.reproduce_depth_2_bug` should correctly resolve and show deeper functions.
</verify>
<done>false</done>
</task>

<task id="4">
<files>
<file>scripts/test_context_depth.py</file>
</files>
<action>
Create a Python verification script that tests:
1. `depth=0`: Target method only.
2. `depth=1`: Target method + direct callees.
3. `depth=2`: Target method + direct callees + indirect callees.
Verify against `/home/hainn/blue/code/cre-test-project`.
</action>
<verify>
Run `python3 scripts/test_context_depth.py` and ensure all assertions pass.
</verify>
<done>false</done>
</task>

<task id="5">
<files>
<file>src/test/java/com/cre/repro/ReproPhase17Test.java</file>
<file>src/test/java/com/cre/repro/FindSymbolTest.java</file>
<file>scripts/repro_bugs.py</file>
</files>
<action>
Remove all temporary reproduction and research artifacts.
</action>
<verify>
Ensure the files no longer exist.
</verify>
<done>false</done>
</task>

## Success Criteria
- `depth=0` returns only the target method.
- No irrelevant methods are included in the output.
- Depth > 2 correctly retrieves deeper callees.
- Verification script passes.
