---
phase: 10-enhanced-xml-wrapping
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified: [src/main/java/com/cre/tools/IntegratedViewBuilder.java, src/main/java/com/cre/tools/GetContextTool.java, src/test/java/com/cre/tools/IntegratedViewBuilderTest.java, src/test/java/com/cre/e2e/RealProjectE2ETest.java]
autonomous: true
requirements: [REQ-10.1]

must_haves:
  truths:
    - "Each file is wrapped in an outer <ClassName.MemberName> tag"
    - "Each class is wrapped in an inner <ClassName> tag"
    - "Package and imports are inside the outer tag but outside the class tag"
    - "GetContextTool correctly identifies the anchor for wrapping"
  artifacts:
    - path: "src/main/java/com/cre/tools/GetContextTool.java"
      provides: "Anchor-aware context retrieval"
    - path: "src/test/java/com/cre/e2e/RealProjectE2ETest.java"
      provides: "E2E verification of hierarchical format"
  key_links:
    - from: "src/main/java/com/cre/tools/GetContextTool.java"
      to: "src/main/java/com/cre/tools/IntegratedViewBuilder.java"
      via: "passing anchor NodeId to build method"
---

<objective>
Implement hierarchical XML wrapping and integrate it with GetContextTool. Each file's output will have nested tags providing clear boundaries for the anchor context and the class definition.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-enhanced-xml-wrapping/10-CONTEXT.md
@.planning/phases/10-enhanced-xml-wrapping/10-RESEARCH.md
@src/main/java/com/cre/tools/IntegratedViewBuilder.java
@src/main/java/com/cre/tools/GetContextTool.java
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Implement Hierarchical Wrapping in IntegratedViewBuilder</name>
  <files>src/main/java/com/cre/tools/IntegratedViewBuilder.java</files>
  <action>
    - Update `build` signature to accept `NodeId anchor`.
    - Derive `ClassName.MemberName` string for the anchor tag (D-01).
    - Insert `<ClassName>` tag around the class definition (D-02).
    - Implement post-processing to wrap the entire file content in the outer `<ClassName.MemberName>` tag.
    - Ensure package declaration and imports stay outside the `<ClassName>` tag but inside the outer tag (D-03).
    - Ensure tags are on their own lines (D-10).
  </action>
  <verify>
    <automated>mvn test -Dtest=IntegratedViewBuilderTest</automated>
  </verify>
  <done>IntegratedViewBuilder supports hierarchical wrapping tags.</done>
</task>

<task type="auto">
  <name>Task 2.2: Update GetContextTool to Pass Anchor Information</name>
  <files>src/main/java/com/cre/tools/GetContextTool.java</files>
  <action>
    - In `execute(nodeIdRaw, depth)`, pass the parsed `startNode` to `builder.build`.
    - In `expand(nodeIdRaw)`, pass the parsed `anchor` to `builder.build`.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>GetContextTool passes anchor information to the builder.</done>
</task>

<task type="auto">
  <name>Task 2.3: Verify Hierarchical Format in E2E Tests</name>
  <files>src/test/java/com/cre/e2e/RealProjectE2ETest.java, src/test/java/com/cre/tools/IntegratedViewBuilderTest.java</files>
  <action>
    - Update `IntegratedViewBuilderTest` to verify the multi-layer hierarchical structure.
    - Update `RealProjectE2ETest.java` assertions to match the new semantic XML format.
    - Run the full test suite to ensure no regressions.
  </action>
  <verify>
    <automated>mvn test</automated>
  </verify>
  <done>Hierarchical format is verified in E2E scenarios.</done>
</task>

</tasks>

<verification>
Check `get_context` tool output visually or via test snapshots to ensure hierarchical tags are correctly nested and imports/package are positioned as required.
</verification>

<success_criteria>
- The context output uses nested tags: `<AnchorTag><Package+Imports><ClassTag>...<ClassTag></AnchorTag>`.
- `RealProjectE2ETest` passes with the new format.
</success_criteria>

<output>
After completion, create `.planning/phases/10-enhanced-xml-wrapping/10-02-SUMMARY.md`
</output>
